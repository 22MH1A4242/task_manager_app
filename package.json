{
  "name": "task-manager-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start-all": "concurrently \"cd backend && uvicorn app:app --reload\" \"cd frontend && npm start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
